import { Component } from "@angular/core";

@Component({
    selector: "app",
    template: "<checkout></checkout>" // add any features component in this tag to check
})

export class AppComponent {



 }




//<router-outlet> </router-outlet> its a placeholder component,
//it serves the component whichever the route provides

// routing mechanism :


//app root only job is to load the feature component

//model and services are reusable data




